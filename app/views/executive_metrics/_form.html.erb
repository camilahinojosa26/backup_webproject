<%= form_with(model: executive_metric) do |form| %>
  <% if executive_metric.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(executive_metric.errors.count, "error") %> prohibited this executive_metric from being saved:</h2>

      <ul>
        <% executive_metric.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
